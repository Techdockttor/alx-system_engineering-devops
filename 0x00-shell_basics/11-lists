#!/bin/bash

# List files in the current directory
echo "Files in the current directory:"
ls -la

# Move up to the parent directory and list files
echo -e "\nFiles in the parent directory:"
cd ..
ls -la

# Move to the /boot directory and list files
echo -e "\nFiles in the /boot directory:"
cd /boot
ls -la
